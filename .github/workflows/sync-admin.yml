# Deprecated — Fratelanza-Admin no longer needs a separate repo.
# The admin app builds from artifacts/fratelanza-admin/ inside the main
# docker-compose.yml on the VPS.
#
# This workflow is kept as a manual-only stub so removing it doesn't
# require destructive git operations. It does nothing.

name: (deprecated) Sync admin subfolder

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is deprecated."
          echo "The admin app now lives in the same repo and is built by docker-compose.yml."
          echo "See deploy/README.md."
